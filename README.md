# store-pos

Local POS system for a retail store.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
python run.py
```

## Debug (browser, no window)

```bash
uvicorn app.main:app --reload
```

## Tests

```bash
pytest
```
